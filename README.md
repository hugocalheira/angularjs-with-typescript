## Simple Register - C.R.D.
AngularJS 1.6 with TypeScript, Bootstrap v4, Sass, LocalStorage and Gulp.

`Create`, `Read` and `Delete`.
Sorry, `update` was not implemented, yet.

### Why AngularJS with TypeScript?
Just because!

## Installation
```bash
npm install
```

## Run
```bash
gulp watch
```
